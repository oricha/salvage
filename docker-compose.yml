
services:
  db:
    image: postgres:16
    container_name: carSalvageDB
    environment:
      - POSTGRES_DB=car_savage
      - POSTGRES_USER=portal
      - POSTGRES_PASSWORD=portal
    ports:
      - "5434:5432"
    volumes:
      - db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  db-data:


